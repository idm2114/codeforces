/*
*
* author: ian macleod
*
*/

#include <bits/stdc++.h>

using namespace std;

int main() { 

	int lines, a, b, c, d;
	cin >> lines;
	for (int i = 0; i < lines; i++) {
		cin >> a >> b >> c >> d;
		int x = b;
		int y = c;
		int z = c;
		cout << x << " " << y << " " << z <<endl;
	}
}

